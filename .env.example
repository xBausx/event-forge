# ==============================================================================
# Example Environment Variables for Event-Forge
# ==============================================================================
#
# Instructions:
# 1. Copy this file to a new file named `.env` in the project root.
# 2. Fill in the values for your local development environment.
# 3. The `.env` file is listed in `.gitignore` and will NOT be committed.
#
# These variables are used by various scripts and by Terraform when running locally.
# For CI/CD (GitHub Actions), these should be set as repository secrets.

# --- AWS Credentials ---
# Used by the `aws` and `terraform` CLIs.
# For the `dev` environment, as provided by your backend team.
AWS_ACCESS_KEY_ID_DEV="YOUR_AWS_ACCESS_KEY_ID_HERE"
AWS_SECRET_ACCESS_KEY_DEV="YOUR_AWS_SECRET_ACCESS_KEY_HERE"
AWS_REGION="ap-southeast-1"

# --- Inngest ---
# Your Inngest Cloud signing key, for securing the webhook endpoint.
# Get this from your Inngest Cloud dashboard.
INNGEST_SIGNING_KEY="YOUR_INNGEST_SIGNING_KEY_HERE"

# The Inngest webhook URL that Google will call.
# This is provided by Inngest when you deploy your functions.
# It is also used by the `add_sheet_watch.sh` script.
INGEST_WEBHOOK_URL="YOUR_INGEST_WEBHOOK_URL_HERE"

# --- Google ---
# The ID of a Google Sheet you want to test with locally.
# Used by the `add/remove_sheet_watch.sh` scripts.
GOOGLE_SHEET_ID="YOUR_TEST_GOOGLE_SHEET_ID_HERE"

# --- Adobe API Concurrency ---
# This is passed to the Inngest `generatePoster` function to control concurrency.
# It should match the value in your config/<env>.yaml file.
ADOBE_CONCURRENCY_LIMIT="5" # Dev limit

# --- Application Environment ---
# Tells all scripts and code which configuration to use (dev, staging, or prod).
APP_ENV="dev"